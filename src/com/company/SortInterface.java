/**
 * Sort Interface interface
 *
 * Because all the algorithms will need a 'sort' method to actually sort the array, I've decided
 * to make an interface which will have the sort method.
 * This means that classes which implements this interface will need to adopt the the sort method.
 *
 */

package com.company;

interface SortInterface
{
    void sort();
}
